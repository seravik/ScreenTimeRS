# ScreenTime RS v0.5.1 构建说明

- 新增使用条款与隐私政策页面。
- 移除“后台继续记录使用时间”设置项。
- 新增预设主题色与自定义动态取色。
- 自定义强调色保存到 `HKCU\Software\ScreenTimeRS\AccentColor`。
- Windows x64 安装包名称：`ScreenTimeRS-v0.5.1-Setup.exe`。

- 隐私与使用条款文档：`TERMS.md`、`PRIVACY.md`。


## XAML 编译修复
- 修复 WinUI 3 `Application.Resources` 中同时直接放置 `XamlControlsResources` 与应用资源导致的 `WMC9999`。
- 将 `XamlControlsResources` 移入 `ResourceDictionary.MergedDictionaries`，应用自定义画刷继续保留在同一资源字典中。
- 该写法符合 WinUI 资源字典的推荐结构。

## 编译修复 v2
- 修复 `ThemeManager.cs` 使用未导入的 `Colors` 类型导致的 CS0103。
- 修复 `Pages.cs` 调用 `UiHelpers.AccentBrush()` 但 `UiHelpers` 未提供该方法导致的 CS0117。
- `WMC1509/WMC9999` 为上述 C# 编译失败后引发的 XAML 编译连锁错误，随 C# 错误修复后应一并消失。

### UI accent follow-up
- 修复“设置 → 常规 → 登录 Windows 后在后台静默启动”复选框未跟随当前主题色的问题。
- 修复“概览 → 正在记录使用时间”信息栏仍固定使用绿色成功色的问题，使其成功状态随当前主题色显示。
- 版本保持 v0.5.1。


## UI 主题色全局同步修复
- 修复自定义主题色只在部分页面/控件生效的问题。
- 补齐 WinUI Fluent accent、NavigationView selection indicator、CheckBox、ComboBox 下拉选择状态等资源。
- 修复“设置 → 常规 → 登录 Windows 后在后台静默启动”复选框仍显示系统蓝色的问题。
- 修复“设置 → 语言”下拉列表仍使用系统蓝色选择标记的问题。
- 修复“概览 → 正在记录使用时间”仍显示系统 Success 绿色的问题，使其跟随当前主题色。
- 修复左侧导航栏选中指示条仍显示系统蓝色的问题。

## v0.5.1 UI Theme Global Fix v7
- 修复 `Pages.cs` 中 `ApplyComboBoxAccent` 定义作用域错误导致的 CS0103 编译失败。
- 将 ComboBox 强调色辅助方法放入 `SettingsPage`，与设置页的主题色应用逻辑保持一致。
- 版本保持 v0.5.1。


## 主题刷新修复
- 新增“应用主题”按钮。
- 动态重应用主题时同时更新 NavigationView 与 NavigationViewItem 局部资源。
- 强制刷新导航栏选中状态与展开状态，避免动态切换主题后出现系统默认颜色残留。
## 主题实时应用修复
- 移除“应用主题”按钮，恢复主题色点击后直接生效的交互。
- 修复 NavigationView 左侧导航选中指示条在运行时仍保留旧 Accent 色的问题。
- 主题色变化时重新挂载现有导航项并恢复当前选中项与导航栏宽度，使导航指示色无需重启窗口即可同步。
