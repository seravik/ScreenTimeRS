# ScreenTime RS v0.4.1 构建说明

## 本版本重点
- 修复“概览 → 最近 14 天”时间轴鼠标悬浮提示无法正常显示的问题。
- 重做“概览 → 最近 14 天”为极简 14 日时间轴。
- 每个日期时间条支持鼠标悬浮显示当日具体使用时长。
- Windows 登录后可通过 Rust 采集器后台静默启动，不自动打开主窗口。
- 优化 WinUI 实时刷新，只更新当前显示页面，减少不必要的 UI 重建。
- Rust 采集器改用轻量 System 初始化，降低后台资源占用。
- 发布配置关闭调试符号与文档产物，并清理旧的 dist\ui 后再发布，减少残留文件影响。

## 输出
- Windows x64：`dist\ui\ScreenTimeRS.UI.exe`
- 安装包：`installer-output\ScreenTimeRS-v0.4.1-Setup.exe`

## Recent hover fix
- Replaced the unreliable WinUI `ToolTipService` usage on the Recent 14 days chart with a persistent in-page hover card.
- Each day now uses the full 48px chart column as the hit area instead of only the visible bar.
- The hover card is non-interactive, stays visible while the pointer remains over the day column, and is refreshed without blinking during the 1-second snapshot updates.
- Hover text follows the selected Chinese / English language.
