# ScreenTime RS v0.3.0 构建说明

## 本版本
- 最近 14 天悬浮提示
- 应用使用时间支持今天、本周、本月、近半年、近一年
- 今日应用排行显示应用图标
- 左侧导航栏支持拖动调整宽度并持久化
- 安装程序支持中文/英文界面，默认安装到 Program Files

## 构建
```powershell
.\build-windows.bat
.\build-installer.bat
```

安装包：
`installer-output\ScreenTimeRS-v0.3.0-Setup.exe`


## 编译修复
- 修复 `MainWindow.xaml.cs` 缺少 `BitmapImage` 命名空间导致的 CS0246 编译错误。
- 启用 C# Nullable 上下文，消除 CS8632 警告。
- 由 CS0246 引发的 XAML 编译器 WMC1509/WMC9999 连带错误随之消除。

## Inno Setup 中文语言文件修复
- 将 `ChineseSimplified.isl` 随项目源码放入 `installer/Languages/`。
- 安装脚本改为引用项目内语言文件，不再依赖本机 Inno Setup 的中文语言包。
- 保留中文和 English 双语言安装界面。

## 安装脚本修复
- 修复 Inno Setup `[CustomMessages]` 语言前缀格式错误。
- 修正中文和英文自定义安装任务提示，避免 `Unknown language name "ShortcutGroup"` 编译错误。

## 语言设置
- 设置页面新增中文 / English 语言选择。
- 语言选择保存到 `HKCU\Software\ScreenTimeRS\LanguageMode`，重新启动后自动恢复。
- 切换语言后无需重启，主要界面会即时更新。
